name: Upload Website

on:
  push:
    branches:
    - master

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    - uses: jsjohnstone/s3-sync-action@master
      with:
        args: --acl public-read --follow-symlinks --delete --exclude '.git/*'
      env:
        AWS_S3_BUCKET: 'jamiejohnstone.com'
        AWS_ACCESS_KEY_ID: 'AKIA5JTKFPUQ7TQ55RVE'
        AWS_SECRET_ACCESS_KEY: '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'
        AWS_REGION: 'eu-west-2'   # optional: defaults to us-east-1
